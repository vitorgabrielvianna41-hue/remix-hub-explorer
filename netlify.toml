[build]
  command = "bun run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — TanStack Router faz o routing no client
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
